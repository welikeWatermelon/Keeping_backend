#name: Deploy to EC2
#
#on:
#  push:
#    branches: [ "main" ]
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#    # 1. 코드 가져오기
#    - name: Checkout code
#      uses: actions/checkout@v4
#
#    # 2. Java 21 세팅
#    - name: Set up JDK 21
#      uses: actions/setup-java@v4
#      with:
#        java-version: '21'
#        distribution: 'temurin'
#
#    # 3. Gradle 권한 부여
#    - name: Grant execute permission for gradlew
#      run: chmod +x gradlew
#
#    # 4. 빌드 (테스트 제외)
#    - name: Build with Gradle
#      run: ./gradlew clean build -x test
#
#    # 5. 서버로 파일 전송 (SCP)
#    - name: Transfer JAR to EC2
#      uses: appleboy/scp-action@master
#      with:
#        host: ${{ secrets.EC2_HOST }}
#        username: ${{ secrets.EC2_USERNAME }}
#        key: ${{ secrets.EC2_KEY }}
#        source: "build/libs/keeping-0.0.1-SNAPSHOT.jar"
#        target: "/home/ubuntu/"
#        strip_components: 2
#
#    # 6. 서버 접속해서 실행 (SSH)
#    - name: Deploy to EC2
#      uses: appleboy/ssh-action@master
#      with:
#        host: ${{ secrets.EC2_HOST }}
#        username: ${{ secrets.EC2_USERNAME }}
#        key: ${{ secrets.EC2_KEY }}
#        script: |
#          sudo fuser -k 8080/tcp || true
#
#          sleep 5
#
#          nohup java -jar \
#            -Dspring.profiles.active=prod \
#            -Dserver.tomcat.remoteip.protocol-header=X-Forwarded-Proto \
#            -Dserver.tomcat.remoteip.remote-ip-header=X-Forwarded-For \
#            /home/ubuntu/keeping-0.0.1-SNAPSHOT.jar \
#            --spring.config.location=/home/ubuntu/application-prod.yml \
#            > /home/ubuntu/nohup.out 2>&1 &
